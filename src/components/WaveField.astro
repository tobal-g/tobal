---
---

<canvas id="wave-field" aria-hidden="true"></canvas>

<script>
  import { initWaveField } from "../scripts/wave-field";

  const canvas = document.getElementById("wave-field") as HTMLCanvasElement;
  if (canvas) {
    initWaveField(canvas);
  }
</script>

<style>
  canvas {
    position: fixed;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 0;
  }
</style>
